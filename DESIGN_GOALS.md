# Infinity Cell 设计目标文档

## 1. 项目定位

Infinity Cell 是面向 Minecraft 1.12.2 / Forge / Applied Energistics 2 的存储扩展模组。项目目标不是简单复制 AE2 原版存储元件，而是在保留 AE2 网络交互习惯的前提下，提供更大容量、更高性能、更适合后期整合包使用的深度存储元件。

项目围绕三类存储对象展开：

- 物品存储元件
- 流体存储元件
- 气体存储元件，依赖 Mekanism Energistics / mekeng 环境

容量阶梯覆盖：

- 1K / 4K / 16K / 64K
- 256K / 1024K / 4096K / 16384K，依赖 NAE2 组件环境
- INF 无限级

## 2. 核心设计目标

### 2.1 提供高阶梯、低心智负担的 AE2 存储扩展

项目希望玩家仍然按 AE2 原有方式理解和使用存储元件：

- 存储元件可插入 ME Drive、ME Chest 等 AE2 宿主。
- 元件保留 AE2 的容量、类型、状态灯、tooltip、JEI 查看等交互概念。
- 配方采用“高级外壳 + 对应组件 = 高级存储元件”的低学习成本结构。
- 空盘可通过潜行右键拆回外壳和组件，降低误合成和升级试错成本。

设计重点是增强 AE2 存储体系，而不是替换 AE2 网络。

### 2.2 突破原版元件的容量与种类限制

原版 AE2 存储元件受容量、类型数和 ItemStack NBT 容量约束影响，在大后期整合包中容易暴露几个问题：

- 高种类物品会快速占满类型限制。
- 大量单种资源需要堆叠大量元件，维护成本高。
- 超大存量和超多 NBT 变体会放大序列化、同步和保存压力。

Infinity Cell 的目标是：

- 有限盘按容量阶梯限制总字节数，但不再施加传统小类型数上限。
- 无限盘不对总容量和总类型数施加实际限制。
- 无限盘对单种存量设置安全上限，避免 long 溢出导致显示、排序或网络同步异常。

### 2.3 将真实数据从 ItemStack NBT 中剥离

项目的关键架构目标是避免把海量存储内容写入元件 ItemStack 自身 NBT。

当前设计采用：

- 每块元件分配独立 `disk_uuid`。
- ItemStack 只保存 UUID 和少量展示统计数据。
- 真实存储内容通过 `WorldSavedData` 落到主世界 `data/infinite/{UUID}.dat`。
- 客户端不直接读取世界存储文件，只通过 NBT 统计或网络包获得展示数据。

这一目标解决的是稳定性问题，而不是单纯的数据组织问题：

- 降低 ItemStack 在背包、网络、容器、掉落物之间流转时携带巨量 NBT 的风险。
- 避免客户端和服务端频繁同步完整存储内容。
- 降低坏档、包体过大、GUI 卡顿和存档膨胀的概率。

### 2.4 面向海量类型和海量数量优化性能

项目应能支撑后期环境中的极端存储场景，例如大量 NBT 变体、巨量单种资源和频繁 IO Port 搬运。

当前性能目标包括：

- 使用 FastUtil `Object2LongMap` 记录 `IAEStack -> count`，让单项存取接近 O(1)。
- 保存时维护 NBT 缓存和 dirty 集合，只重新序列化变动项。
- 存取时维护总字节、总类型、总数量统计，避免每次查询都全量遍历。
- 客户端 JEI 视图最多请求和显示 63 个条目，避免把完整盘内容塞进网络包。
- 空数据可清理，减少无效 `.dat` 文件对存档目录和加载过程的影响。

性能设计的核心判断是：服务端存储操作可以承受局部 map 更新，但不能接受频繁全量重建、全量同步或把所有内容塞入 ItemStack NBT。

### 2.5 保持 AE2 行为兼容

项目通过自定义 `ICellHandler` 接管本模组元件，但对外仍暴露 AE2 期望的存储接口。

兼容目标包括：

- 支持 AE2 物品通道和流体通道。
- 在 mekeng 存在时支持气体通道。
- 有限盘按 AE2 的单位/字节概念计算容量。
- 无限盘返回安全的展示容量和剩余容量，避免 AE2 上层误判为已满。
- 通过驱动器状态灯表达空盘、正常、临界、已满状态。
- 保留 AE2/JEI 的查询习惯，允许玩家查看盘内内容概览。

兼容不是盲目复用原版逻辑。对于原版或 NAE2 在大数下容易溢出的地方，项目会用自定义逻辑替代。

### 2.6 可选依赖按存在情况启用

项目目标是在不同整合包环境下尽量优雅降级。

依赖策略：

- AE2 是必需依赖。
- MixinBooter 是必需依赖，用于兼容性修正。
- NAE2 是可选能力来源，只在存在时注册 256K 到 16384K 阶梯。
- mekeng 是可选能力来源，只在存在时注册气体元件和气体无限组件。
- NAE2 相关 Mixin 只在目标类存在时加载，避免缺少可选依赖导致启动崩溃。

该目标可以让模组覆盖更广的 1.12.2 AE2 整合包环境，而不是绑定单一大型包配置。

### 2.7 提供可理解、可诊断的玩家反馈

项目需要让玩家知道元件处于什么状态，并能在出问题时定位到具体盘。

当前反馈设计包括：

- Tooltip 显示 UUID 或未分配状态。
- 有限盘显示已用字节、总字节和类型数。
- 无限盘显示类型数和无限容量概念。
- JEI 提供“元件内容”分类，显示已存资源样本、数量和容量摘要。
- 服务端命令 `/cleanemptycells` 可清理空后端数据文件。
- 日志输出关键注册、删除数据文件和 JEI 异常。

UUID 暴露在 tooltip 中是有意设计：它牺牲少量界面简洁度，换来服务端排障和数据文件定位能力。

## 3. 架构原则

### 3.1 元件物品只做身份与配置，后端数据独立持久化

`AdvancedCellItem` 负责元件身份、类型、阶梯、tooltip、配方反查和 UUID 分配。

`AdvancedCellData` 负责真实存储数据。

这条边界应保持稳定：

- 不应把完整存储内容重新塞回 ItemStack NBT。
- 不应让客户端直接访问服务端世界数据文件。
- 不应让不同 UUID 的盘共享同一份后端数据。

### 3.2 有限盘和无限盘分离容量策略

`AdvancedCellInventory` 和 `InfiniteCellInventory` 分离是必要设计。

有限盘目标：

- 遵守容量上限。
- 支持部分接收和退回无法存入的数量。
- 按使用率返回绿、橙、红、蓝状态。

无限盘目标：

- 不进行总容量扣减判断。
- 始终对 AE2 报告足够剩余空间。
- 单种数量设置安全上限，避免溢出。
- 永不显示橙色或红色满盘状态。

这能避免在一个类里堆叠大量条件分支，也能降低无限盘因有限盘容量算法误伤而变成“伪有限盘”的风险。

### 3.3 服务端权威，客户端只缓存展示快照

客户端 JEI 和 tooltip 只能使用：

- ItemStack 上同步过来的统计 NBT。
- 服务端返回的受限快照。
- 本地短期缓存。

服务端响应客户端请求时，会重新在玩家背包中查找匹配 UUID 的真实元件，而不是完全信任客户端传来的 ItemStack。

这一设计目标是：

- 防止客户端构造任意 UUID 读取不存在或不属于自己的数据。
- 控制网络包大小。
- 允许 JEI 在有缓存时立即显示，在缓存过期后异步刷新。

### 3.4 对大数运算保持保守

项目大量接触 long 级别数量。设计上应避免：

- `long` 相加直接溢出。
- `long` 转 `int` 造成崩溃或负数。
- 用减法结果作为排序比较值。
- 在无限盘容量显示中用“总容量 - 已用容量”判断剩余空间。

当前实现通过以下方式降低风险：

- 无限盘使用 `Long.MAX_VALUE / 2` 作为展示和单种上限。
- 总字节和总数量使用 overflow 计数辅助显示。
- 排序使用 `Long.compare`。
- 面向 JEI 的数据返回数量受限。

## 4. 非目标

以下内容不是当前项目优先目标：

- 不做独立于 AE2 的全新存储网络。
- 不提供跨维度、跨服务器或数据库级远程存储。
- 不把元件设计成可堆叠物品。
- 不支持把本模组存储元件再放入其他存储元件。
- 不追求完全模拟 AE2 原版类型惩罚机制，当前设计明确将每类型字节损耗设为 0。
- 不在未安装 mekeng 时强行注册气体盘。
- 不在未安装 NAE2 时强行注册 256K 以上有限阶梯。
- 不让 JEI 展示盘内全部内容，JEI 只提供摘要和前若干项预览。

## 5. 主要用户场景

### 5.1 后期大规模资源集中存储

玩家希望把石头、圆石、矿物、产线中间产物、流体或气体集中到少量高阶元件中，而不是维护大量原版 AE2 存储盘。

设计响应：

- 多阶梯容量覆盖从早期到后期。
- 无限盘用于终局资源池。
- 状态灯和 tooltip 帮助判断容量压力。

### 5.2 大量 NBT 变体存储

整合包中常见带 NBT 的工具、芯片、样板、中间件或测试物品。传统类型限制容易失效。

设计响应：

- 有限盘取消传统小类型上限。
- 后端按 `IAEStack` 键直接记录数量。
- 使用 FastUtil 和增量序列化降低多类型成本。

### 5.3 玩家查看某块盘里有什么

玩家可能手持或在 JEI 中查看元件，想知道盘中主要内容。

设计响应：

- JEI 自定义分类展示最多 63 项。
- 先用本地缓存快速显示，再异步请求服务端最新快照。
- 保留 JEI 的 R/U 查询交互能力。

### 5.4 服务端清理和排障

服务器可能产生空 UUID 数据文件，或者需要定位某块异常元件。

设计响应：

- 每块盘 tooltip 显示 UUID。
- 空盘拆分会尝试删除对应 `.dat` 文件。
- `/cleanemptycells` 可批量清理空数据文件。

## 6. 约束与风险

### 6.1 版本约束

项目绑定 Minecraft 1.12.2、Forge 14.23.5.x 和 AE2 1.12.2 API。AE2、NAE2、mekeng 的内部实现差异会影响兼容性。

### 6.2 反射注入风险

主类通过反射把 `AdvancedCellHandler` 插入 AE2 cell registry 的处理器列表首位。这样能确保本模组元件优先被自定义 handler 接管，但也依赖 AE2 内部字段结构。

缓解方式：

- 反射失败时回退到普通 `addCellHandler`。
- 日志记录注入结果。

### 6.3 世界数据文件生命周期风险

数据从 ItemStack NBT 剥离后，UUID 数据文件生命周期必须谨慎管理。

潜在问题：

- 玩家丢弃空盘后留下空 `.dat` 文件。
- 删除空盘数据时，已加载的 `WorldSavedData` 可能在世界保存时重新写回。
- UUID 丢失会导致元件无法关联旧数据。

当前缓解方式：

- 空盘拆分时删除物理文件并清理 dirty 状态。
- 提供 `/cleanemptycells` 命令。
- UUID 懒分配，减少创造物品栏无意义 UUID。

### 6.4 JEI 与大数显示风险

NAE2 的盘内 JEI 预览对超大 long 数量存在溢出风险。

当前策略：

- 对本模组元件禁用 NAE2 原预览路径。
- 注册自定义 JEI 分类。
- 排序和显示全部走 long 安全路径。
- 网络返回条目数固定上限。

### 6.5 气体通道兼容风险

气体通道来自外部模组，类型判断依赖可选依赖环境。当前代码显式检查 MekEng 的 `IGasStorageChannel` 接口，避免把其他非物品、非流体 AE2 通道误判为气体。

未来如果更换气体集成模组，需要在通道匹配工具中增加对应接口名或适配层。

## 7. 验收标准

项目达到设计目标时，应满足以下标准：

- 装有 AE2 时模组可启动并注册 1K 到 64K 的物品、流体元件和 INF 物品、流体元件。
- 装有 NAE2 时额外注册 256K 到 16384K 阶梯。
- 装有 mekeng 时额外注册气体元件和气体无限组件。
- 每块进入玩家背包的元件能获得唯一 UUID。
- 存入内容不会写入 ItemStack 的完整 NBT，只通过 UUID 关联世界数据。
- 有限盘在容量不足时正确部分接收并返回剩余输入。
- 无限盘在常规 AE2 网络操作中不会因展示容量被误判为满。
- 盘内数据保存后重进世界仍可读取。
- 空盘拆分能返还外壳和对应组件。
- JEI 查看不会因为大数量排序或显示触发整数溢出。
- 客户端请求盘内数据时，服务端只返回玩家实际持有且 UUID 匹配的元件数据。
- `/cleanemptycells` 能清理空后端数据文件。

## 8. 后续演进方向

后续如果继续扩展，建议优先围绕稳定性和可观测性推进：

- 增加自动化测试或调试命令，覆盖有限盘部分接收、无限盘大数、防溢出和持久化恢复。
- 为 UUID 数据文件增加更明确的迁移或修复工具。
- 如果未来支持其他气体集成模组，为气体通道匹配增加可配置或多接口适配。
- 为 JEI 视图增加分页或搜索，但仍应保持网络返回上限和服务端权威。
- 增加配置项控制无限盘是否耗电、有限盘容量倍率、JEI 返回数量上限等可平衡参数。
